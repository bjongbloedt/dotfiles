# This is Git's per-user configuration file.
[user]
name = bjongbloedt
email = bjongbloedt@users.noreply.github.com 

[includeIf "gitdir:~/workspace/foodsby/"]
path = ~/workspace/foodsby/.gitconfig

[core]
excludesfile = ~/.gitignore

# Short version of git status
st = status --short --branch

[pull]
	rebase = false
